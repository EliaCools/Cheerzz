<form method="post" action="{{ path('shopping_line_delete', {'id': shoppingLine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shoppingLine.id) }}">
    <button class="btn">Remove</button>
</form>
