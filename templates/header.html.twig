

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><img class="logo" src="{{ asset('images/logo.svg') }}" alt="logo"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">HOME</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('about_us') }}">ABOUT US</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    COCKTAILS
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('product_index') }}">OUR PRODUCTS</a>
                    <a class="dropdown-item" href="{{ path('cocktail_index', {'firstCharacter' : 'a'}) }}">SEARCH PRODUCTS</a>
                    <a class="dropdown-item" href="{{ path('home_brew_new') }}">MAKE YOUR OWN COCKTAIL</a>
                    <a class="dropdown-item" href="{{ path('bartender_page') }}">BARTENDER</a>
                    <div class="dropdown-divider"></div>
                </div>
            </li>
        </ul>
        <div class="navbar-nav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">CONTACT</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">LOGOUT</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">SIGN IN</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">SIGN UP</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('my_shopping_cart') }}"><img class="cart" src="{{ asset('images/cart.svg') }}" alt="cart-logo"></a>

                    <span class="cart-counter">
                     {% if shoppingCart is defined  %}
                         {% if shoppingCart  is not null %}
                             {{ shoppingCart.getTotalProducts() }}
                         {% endif %}
                     {% endif %}
                    </span>

                </li>
            </ul>
            </ul>


        </div>
    </div>
</nav>



