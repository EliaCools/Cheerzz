{% extends 'base.html.twig' %}

{% block title %}HomeBrew{% endblock %}

{% block body %}
    <h1>HomeBrew</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_brew.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ home_brew.name }}</td>
            </tr>
            <tr>
                <th>Glass</th>
                <td>{{ home_brew.glass }}</td>
            </tr>
            <tr>
                <th>Instructions</th>
                <td>{{ home_brew.instructions }}</td>
            </tr>
            <tr>
                <th>IngredientsAndMeasurements</th>
                <td>{{ home_brew.ingredientsAndMeasurements ? home_brew.ingredientsAndMeasurements|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_brew_index') }}">back to list</a>

    <a href="{{ path('home_brew_edit', {'id': home_brew.id}) }}">edit</a>

    {{ include('home_brew/_delete_form.html.twig') }}
{% endblock %}
